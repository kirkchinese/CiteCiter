/** Host half of probe P1. Question: does the row mount at all? Marker on stderr. */
export const name = 'citeciter-probe-p1'
export const inject = []
export function apply(ctx) {
  console.error('[citeciter-probe-p1] host half apply')
}
