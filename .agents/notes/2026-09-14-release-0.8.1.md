# 0.8.1 发布与 0.8.0 弃用

用户在修复验收后明确要求发布 0.8.1，并通过各处告知 0.8.0 在 Linux 上存在符号链接启动错误，弃用旧版并指引升级。本次范围包括默认分支及包内中英文 README、0.8.0/0.8.1 Release 文档、GitHub Release 和 npm 0.8.0 的弃用元数据。

运行代码沿用已验收的 703f9a0，仅更新发布文档。重新打包后核对所有运行产物与已安装候选一致，再使用该包发布 npm 和 GitHub Release。保留 0.8.0 包、标签和历史记录；弃用提示描述 Linux 触发条件，不宣称其他平台全部不可用。

发布前运行类型检查、构建、包内容与 diff 检查，并检查 Windows/Ubuntu CI。Linux 模块加载前后对照和主要 Desktop 真实模型结果见 docs/validation/2026-09-14-symlink-launcher.md；不将静态成功扩展为全平台功能验收。

最终包与已安装候选逐文件比较，118 个文件中仅 README.md 和 README.zh.md 有变化，运行代码与类型声明全部一致。本地类型检查、完整构建和 diff 检查再次通过，最终包哈希记录在同一验收文档中。

0.8.1 已发布为 npm latest，GitHub v0.8.1 为公开正式版及 Latest，标签指向合并提交 dc537a0e47a901b89c8c11f22c65a38e6364c9b5。PR #6 与该合并提交的 Windows/Ubuntu CI 均成功。直接下载 npm 包并计算 SHA-256/SHA-512，结果与本地包及 GitHub Release 附件摘要一致。npm 仅将 0.8.0 标记弃用，0.8.1 未被弃用；旧版 GitHub Release 的标题、正文和升级命令已同步。临时认证文件与进程中的令牌在命令结束后清理。
