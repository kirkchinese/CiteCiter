# 来源窗口语义与原生提示词

用户提交模型自分析 issue，要求核查并优化提示词。临时附件路径已失效，在同一微信文件目录的 msg/file/2026-09 找到同名报告。报告作为待验证材料，未直接按建议恢复旧版静态引文注入。

代码证实 availableThroughSeq 受请求上界约束；它还忽略 fromSeq，并不是严格的区间内最高值。capturedThroughSeq 包含被过滤事件，是扫描游标。新增独立快照上界和续读起点，保留旧字段语义并明确描述，不通过重命名改动持久化数据。

原生路径已有 Citer 身份提示词，缺少的是旧版来源读取说明与建议追问。分别抽出来源工具契约和原生提示词模块，通过已发布 systemPrompt.section / tools.register 接口注册；没有修改宿主循环。工具描述同样包含分页语义，避免只依赖系统段。

用户确认恢复首答建议追问。沿用现有设置与客户端解析，补充三个不同问题、长度与末尾 JSON 格式约束；点击仍只生成草稿。新旧权限和手动来源附件门禁保持原样。源引用的结构化元数据没有偷偷补进系统提示词，避免重新暴露已删除的草稿引用。

真实来源日志通过 DSH 公开只读持久化句柄读取，临时检查均以内联命令执行，不保留脚本、模型替身或测试 Session 副本。主要 Desktop 的真实 DeepSeek 模型自行从短窗口续读到来源末尾；首答追问显示、点击仅入草稿、第二轮不重复、关闭开关及移除来源后拒绝读取均通过。主日志散列未变。详细结果记录在 docs/validation/2026-09-14-source-read-guidance.md。

原生提示词在每次装配时读取当前设置，补正设置页关于生效时间的说明。保持已加载 Agent 的工具授权与生命周期不变。本次准备 0.8.2 候选，未推断为 npm 发布授权；Linux/macOS UI 不列为本轮实测。
